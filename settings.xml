<!-- settings.xml -->
<settings>
    <servers>
        <server>
            <!-- Sonatype Settings -->
            <id>ossrh</id>
            <username>${env.SONATYPE_USERNAME}</username>
            <password>${env.SONATYPE_PASSWORD}</password>
        </server>
    </servers>
</settings>
